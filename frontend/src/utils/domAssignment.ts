export const showBanner = (text: string) => {
  // TODO: Use native DOM APIs (document.createElement, style, etc.)
  // 1. Create a <div> element
  // 2. Set background to 'red' and text color to 'white'
  // 3. Set innerText to the 'text' argument
  // 4. Position it fixed at the top (top: 0, left: 0, width: 100%)
  // 5. Add a click handler to remove it when clicked
  // 6. Append it to document.body
};
